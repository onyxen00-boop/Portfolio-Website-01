rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read; // public assets
      allow write: if false; // tighten until explicit use-case added
    }
  }
}
